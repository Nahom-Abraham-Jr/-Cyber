import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  devIndicators: false, // Disables all floating developer overlays completely
};

export default nextConfig;